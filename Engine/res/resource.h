#pragma once
// Resource identifiers are the resource compiler's, which knows only the preprocessor, so they are defines
// and are named here once for both it and the C++ that asks Windows for them.
#define IDI_APPICON 1
